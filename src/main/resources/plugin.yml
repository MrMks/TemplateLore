name: TemplateLore
version: ${version}
main: com.github.mrmks.mc.template.TemplateLore
depend: [DevToolsB]
softdepend: [PlaceholderAPI]
commands:
  tl:
    permission: tl.perm.cmd.*
permissions:
  tl.perm.cmd.get:
    default: op
  tl.perm.cmd.list:
    default: op
  tl.perm.cmd.reload:
    default: op
  tl.perm.cmd.*:
    default: op
    children: [tl.perm.cmd.get, tl.perm.cmd.list, tl.perm.cmd.reload]