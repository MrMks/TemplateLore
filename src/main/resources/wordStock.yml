WordStockName:
  # weight|content, if the weight is not given, the weight will be 1;
  # you can set the weight to 0 to skip the line
  # the weight can't be generated by tags, just write it directly
  - 1|sdjidsf
  - 1|fjskfjie
  # in this case, the weight is 20, the value is n|, which means skip
  - 20|n|
